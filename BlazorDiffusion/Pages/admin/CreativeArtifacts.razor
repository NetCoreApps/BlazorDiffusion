@page "/admin/creative-artifacts"
@attribute [Authorize(Roles = "Admin")]

@inject NavigationManager NavigationManager

<Breadcrumbs class="mb-8" HomeHref="/admin">
    <Breadcrumb>
        Creative Artifacts
    </Breadcrumb>
</Breadcrumbs>

<AutoQueryGrid Model="CreativeArtifact" Apis="Apis.AutoQuery<QueryCreativeArtifacts,UpdateCreativeArtifact>()" />
